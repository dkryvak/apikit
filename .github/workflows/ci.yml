name: CI

# Tag Flow (see docs/release-flow-setup.md): CI on PRs to main and pushes to main / hotfix/**.
# The job is named `build-and-test` because the main branch ruleset requires that exact check.
on:
  pull_request:
    branches: [main]
  push:
    branches:
      - main
      - "hotfix/**"

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # On the bootstrap branch (no code yet) the Go steps are skipped so CI stays green.
      - name: Check for Go module
        id: gomod
        run: |
          if [ -f go.mod ]; then echo "present=true" >> "$GITHUB_OUTPUT"; else echo "present=false" >> "$GITHUB_OUTPUT"; fi

      - name: Set up Go
        if: steps.gomod.outputs.present == 'true'
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: gofmt
        if: steps.gomod.outputs.present == 'true'
        run: |
          out="$(gofmt -l . | grep -v '^vendor/' || true)"
          if [ -n "$out" ]; then echo "Unformatted files:"; echo "$out"; exit 1; fi

      - name: go vet
        if: steps.gomod.outputs.present == 'true'
        run: go vet ./...

      - name: go test
        if: steps.gomod.outputs.present == 'true'
        run: go test ./...

      - name: go build
        if: steps.gomod.outputs.present == 'true'
        run: go build ./...
